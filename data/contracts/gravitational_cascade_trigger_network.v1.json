{
  "schema_version": "1.0",
  "contract_id": "RLL_GRAVITATIONAL_CASCADE_TRIGGER_NETWORK_V1",
  "status": "HYPOTHESIS_LAB_IMPLEMENTED_CLAIM_BLOCKED",
  "claim_allowed": false,
  "purpose": "Model a finite-speed threshold cascade on a coarse-grained network as a falsifiable laboratory for domino/mousetrap-style gravitational-structural triggering hypotheses.",
  "analogy_boundary": {
    "domino": "topology of sequential threshold crossing only",
    "mousetrap_ping_pong": "a small seed can unlock pre-stored local reservoirs; the seed does not supply the total released energy",
    "nuclear_fission": "chain-reaction topology only; no gravitational microscopic multiplication process is claimed",
    "cosmic_web": "coarse-grained graph representation only; topology is not proof of causal triggering"
  },
  "hard_invariants": [
    "0 < propagation_speed_m_s <= c",
    "0 <= coupling <= 1",
    "sum(outgoing coupling for each source node) <= 1",
    "0 <= release_fraction <= 1",
    "threshold_j >= 0",
    "reservoir_j >= 0",
    "node identifiers are unique",
    "graph fan-out must never duplicate one local energy release",
    "TOKEN_VAZIO is not zero and is never promoted to evidence",
    "branching_potential > 1 is not proof of a physical avalanche"
  ],
  "effective_attenuation": {
    "equation": "A(d)=1/[1+(d/d0)^alpha]",
    "default_alpha": 2.0,
    "interpretation": "effective laboratory attenuation only",
    "forbidden_interpretation": "derived universal GR propagation law"
  },
  "activation_rule": {
    "equation": "Q_i(t)=sum incoming pulses; activate if Q_i >= Theta_i",
    "release": "E_release_i = eta_i E_reservoir_i",
    "transmission": "E_ij = E_release_i K_ij A(d_ij)",
    "coupling_budget": "sum_j K_ij <= 1 for each source i",
    "delay": "Delta t_ij = d_ij / v_prop"
  },
  "observables": ["avalanche_size", "active_fraction", "max_depth", "duration_s", "total_seed_j", "total_released_j", "branching_potential"],
  "promotion_requirements": [
    "identify concrete astrophysical node class",
    "derive threshold from a physical instability or transition",
    "derive reservoir from a bounded physical energy budget",
    "derive coupling from GR/MHD/radiative physics rather than fit it freely",
    "define an observable distinguishable from independent coincident events",
    "run randomized topology and event-time null models",
    "obtain independent replication"
  ],
  "claim_boundary": "The implementation demonstrates a causal threshold-cascade computation, not a demonstrated cosmic avalanche."
}
