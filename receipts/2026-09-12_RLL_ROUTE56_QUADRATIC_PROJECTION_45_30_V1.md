# Route56 Quadratic Projection Bridge 45°→30° — append-only receipt

Date: 2026-09-12
Parent: merged PR #880
Claim allowed: **false**

## Exact identity

For `h²=a²+b²` and `d=h-a`:

`d²+2ad=b²`

equivalently:

`(h-a)(h+a)=b²`.

For the 45° isosceles unit-cathetus case:

`a=b=1`, `h=√2`, `d=√2-1`

and:

`(√2-1)(√2+1)=1`.

## Secondary projection

The coefficient `√3/2=cos30°` is applied only to the residual `d`:

`p=(√3/2)d`, `q=(1/2)d`

with exact norm preservation:

`p²+q²=d²`.

This resolves the former separate-geometric-token gap without claiming that 30° is native to the 45° Route56 mesh.

## Boundary

- exact geometry: YES;
- empirical evidence weight: NO;
- physical interpretation: NO;
- cosmology claim promotion: NO.

`GEOMETRIC_IDENTITY != PHYSICAL_EVIDENCE != SCIENTIFIC_CLAIM`
