# PBIP-L1 — Federated binding into RLL

- `FEDERATION_ID`: `PBIP-L1-FED-V1`
- `FORMULA_ID`: `PBIP-L1`
- `state`: `BOUND_REFERENCE / CLAIM_GATED`
- `claim_allowed`: `false`
- `date`: `2026-09-06`

## Authority boundary

This repository remains authoritative for the RLL cosmological equation, likelihoods, datasets, falsifiers and reproduced scientific results. It is **not** made authority for Euclidean geometry merely by this binding.

The PBIP-L1 relation is imported as a typed mathematical reference only:

```math
q^2=r^2-d_\perp^2,
\qquad
\Delta_B=4(r^2-d_\perp^2)=4q^2.
```

Declared setup: a line with unit direction intersects a Euclidean circle of radius `r`; `d_perp` is the perpendicular distance from the center to the line and `q` is the half-chord. Under this setup, the sign of `Delta_B` classifies secant/tangent/no-real-intersection.

## Federation edges

```text
Matem-tica- / PBIP-L1 formal derivation
  -> Mapa / PBIP-L1-FED-V1 relation authority
  -> ChipQuantum / torus-sphere-Poincare geometry implementation reference
  -> Vectras-VM-Android + Rafaelia_Private / consumer bindings
  -> RafPolimata / evidence route
  -> papers / academic ledger
  -> RLL / typed cross-domain reference only
```

Current upstream review references:

- `rafaelmeloreisnovo/Matem-tica-#23`
- `rafaelmeloreisnovo/ChipQuantum#68`
- `rafaelmeloreisnovo/papers#73`

## Non-equivalence guard

The following implication is forbidden without independent derivation and evidence:

```text
PBIP-L1 Euclidean geometry => RLL cosmological dynamics
```

No physical vortex, cosmological field, likelihood improvement or observational result is inferred from PBIP-L1.

```text
TOKEN_VAZIO_RLL_PBIP_PHYSICAL_BINDING
TOKEN_VAZIO_RLL_PBIP_LIKELIHOOD_BINDING
TOKEN_VAZIO_RLL_PBIP_DATASET_BINDING
```

## Falsifier / promotion rule

Promotion requires an explicit map from PBIP variables to RLL variables, dimensional consistency, a new testable prediction, dataset binding, likelihood implementation and independent reproduction. Until then:

```text
VISÃO != CÓDIGO != ARTEFATO != EXECUÇÃO != EVIDÊNCIA != CLAIM
claim_allowed=false
```

## R3

- `F_ok`: PBIP-L1 has a typed RLL reference without changing the RLL model.
- `F_gap`: no physical/cosmological binding is evidenced.
- `F_next`: consume only an evidence receipt carrying the same `FEDERATION_ID` and explicit RLL mapping.
